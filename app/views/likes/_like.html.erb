
<!-- like機能 -->
          <% if user_signed_in? %>
              <% if post.like_user(current_user.id) %>

                <%= link_to post_like_path(like, post_id: post.id), method: :delete, id: "like-button", remote: true do %>

                  <%= image_tag("like-button.png",:width =>"40",:height => "40") %>

                <% end %>
                <span>
                  いいね<%= post.likes_count %>件
                </span>
              <% else %>
                <%= link_to post_likes_path(post),id: "like-button", remote: true do %>
                  <%= image_tag("liked-button.png",:width =>"40",:height => "40") %>

                <% end %>
                <span>
                  いいね<%= post.likes_count %>件
                </span>
              <% end %>
            <% end %>
